name: Generate podcast feeds
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      # - name: Setup Python
      #   uses: actions/setup-python@v4
      #   with:
      #     python-version: '3.11'
      # - name: Install Dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install pyyaml
      # - name: Run feed Generator
      #   run: python feed.py
      # - name: Push Repo
      #   run: |
      #     git config user.name githubActions1
      #     git config user.email ojaswigupta317@gmail.com
      #     git add .
      #     git commit -m "Modified feed"
      #     git push
      # commented these out as already doing this in the generator repo their 
      - name: Run Feed Generator
        uses: Ojaswi-Gupta/podcast-generator@main
        
